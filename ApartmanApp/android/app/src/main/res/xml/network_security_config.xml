<?xml version="1.0" encoding="utf-8"?>
<!--
  Cleartext (HTTP) trafiği yalnızca geliştirme host'larına izin verilir.
  Üretimde API_BASE_URL https:// olmalıdır.

  - 10.0.2.2          → Android emülatör host loopback
  - localhost / 127.0.0.1 → simulator/desktop
  - 192.168.x.x       → ev/ofis LAN'ında fiziksel cihaz testi (gerekirse genişlet)
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <!-- LAN'da fiziksel cihaz testi gerekirse buraya alt ağı ekleyin:
        <domain includeSubdomains="true">192.168.1.1</domain>
        -->
    </domain-config>
</network-security-config>
